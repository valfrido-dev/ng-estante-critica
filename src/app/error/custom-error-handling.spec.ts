import { CustomErrorHandling } from './custom-error-handling';

describe('CustomErrorHandling', () => {
  it('should create an instance', () => {
    expect(new CustomErrorHandling()).toBeTruthy();
  });
});
